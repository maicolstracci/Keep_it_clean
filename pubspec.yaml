name: keep_it_clean
description: Keep it clean. Trova il cestino per la raccolta differenziata più vicino a te.

version: 1.2.1+13

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  intl:
  cupertino_icons: 1.0.5
  google_maps_flutter: ^2.0.3
  permission_handler: ^10.2.0
  shared_preferences: ^2.0.15
  firebase_core: ^1.0.2
  cloud_firestore: ^3.5.1
  firebase_storage: ^10.3.11
  firebase_auth: ^3.11.2
  google_sign_in: ^5.0.1
  location: 5.0.0-dev.8
  url_launcher: ^6.0.3
  path_provider: ^2.0.11
  http:
  image_picker: 0.8.6
  auto_size_text: 3.0.0
  path:
  flutter_launcher_icons:
  smooth_page_indicator: 1.0.0+2
  flutter_device_type: ^0.4.0

  ### REFACTORING ###
  stacked: 3.0.0
  auto_route: 5.0.2
  stacked_services: 0.9.8
  get_it: 7.2.0
  injectable: 1.5.4
  geoflutterfire: ^3.0.3
  flutter_hooks: 0.18.5+1
  stacked_hooks:
  observable_ish:
  easy_localization: ^3.0.0
  tutorial_coach_mark: 1.2.4
  device_info: 2.0.3
  flutter_bloc: 8.1.1
  shimmer: 2.0.0
  rive: ^0.9.1
  the_apple_sign_in:
  firebase_core_platform_interface: 4.5.1
  firebase_messaging: ^13.0.4


flutter_icons:
  android: true
  ios: true
  image_path: "assets/keep_it_clean_logo.png"
  image_path_ios: "assets/keep_it_clean_logo_ios.png"
#  adaptive_icon_background: "#FF5B9C64"
  adaptive_icon_background: "#FF449E64"
  adaptive_icon_foreground: "assets/keep_it_clean_logo_adaptive.png"

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic:
  build_runner:
  auto_route_generator: 5.0.3
  injectable_generator: 1.5.5

# The following section is specific to Flutter.
flutter:
  assets:
    - assets/illustrations/
    - assets/icons/
    - assets/maps_markers/
    - assets/translations/
    - assets/loading.gif
    - assets/keep_it_clean_logo.png
    - assets/keep_it_clean_logo_ios.png
    - assets/keep_it_clean_logo_adaptive.png
    - assets/keep_it_clean_only_logo.png
    - assets/no-avatar.jpg
    - assets/button_animation.riv

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: CustomIcons
      fonts:
        - asset: fonts/CustomIcons.ttf


    - family: Montserrat
      fonts:
        - asset: fonts/Montserrat-Bold.ttf
          weight: 700
        - asset: fonts/Montserrat-Light.ttf
        - asset: fonts/Montserrat-Medium.ttf
        - asset: fonts/Montserrat-SemiBold.ttf

